services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./assets:/app/assets
      - ./state:/app/state
    restart: unless-stopped
